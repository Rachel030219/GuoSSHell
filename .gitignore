# 构建产物
target/
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies

# Flutter / iOS
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh
ios/Pods/
ios/.symlinks/
*.xcuserstate
xcuserdata/
DerivedData/
*.ipa
*.dSYM.zip

# Rust
# 注意：**不**忽略 Cargo.lock。本仓库产出的是 App（不是库），而且上游是 pin 到
# 具体 rev 的 git 依赖 —— lock 文件是构建可复现性的一部分，必须进版本控制。
# 顺带：不要忽略 *.xcodeproj 的 project.pbxproj、也不要忽略 ios/ 下的配置模板。

# macOS
.DS_Store

# IDE
.idea/
*.iml

# Rust related
/.cargo/
/target/

# Generated signals
/lib/src/bincode
/lib/src/serde
/lib/src/bindings
