# AGENTS.md — GuoSSHell 工程规则

## Followup 记录（2026-09-22 起）

「存在但不马上修」的问题，一律在 `docs/followups/YYYYMMDD_问题简述.md` 建条目跟进：

- 条目**入库**，但不得包含本机信息：真实 IP、端口、用户名、设备名、绝对路径——
  用「局域网」「非标准端口」这类描述代替
- 只描述问题、现状与下一步，不写修复过程
- commit message 里指向条目即可，不展开细节

## 提交与入库文档：只写最终状态

commit message 与入库文档（README 等）只描述**最终状态**。修复过程、踩坑经过、
中间结论不入库（需要留痕的进 followup 条目，其余留在会话记录里）。
